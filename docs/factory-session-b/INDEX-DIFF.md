# INDEX.md — Diff für Session-B-Einträge

Manuell in das `niklasc0/Website-KB` Repo übernehmen, nach
`INDEX.md` → unter „Tag-Map", an passender Stelle alphabetisch
einsortieren.

## Neue Zeilen für die Tag-Map

```md
| `#design-system`, `#tokens`, `#claude-design`, `#schema` | [templates/design-system.spec.md](templates/design-system.spec.md) | Schema-Vertrag für `design-system.json` |
| `#prompt`, `#claude-design`, `#template` | [templates/claude-design-prompt.md](templates/claude-design-prompt.md) | Self-contained Prompt für claude.ai/design |
| `#playbook`, `#design-system`, `#consume`, `#tokens` | [playbooks/design-tokens-to-project.md](playbooks/design-tokens-to-project.md) | Wie Claude Code die JSON in Theme-CSS-Vars übersetzt |
```

## Alphabetische Einsortierung (Hilfe)

Die Tag-Map ist vorerst grob alphabetisch. Empfohlene Position:

- `#design-system, #tokens, ...` direkt nach den `#css`-Einträgen.
- `#prompt, #claude-design, ...` nach `#playbook`-Einträgen.
- `#playbook, #design-system, #consume, ...` zwischen den anderen `#playbook`-Einträgen.

## Neuer Tag-Index (zum Ergänzen, falls du eine eigene Liste pflegst)

Neu eingeführte Tags durch Session B:
- `#design-system`
- `#tokens` (war schon, aber neue Häufung)
- `#claude-design`
- `#schema`
- `#prompt`
- `#consume`
